<%= render 'layouts/errors', :object => @task %>

<%= form_for [@list, @task] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <br><br>
  <%= f.label :done %>
  <%= f.select :done, [["Not done", false], ["Done", true]] %>
  <p><%= f.submit %></p>
<% end %>
